<nav>
   <!-- Mobile Navbar -->
   <div
      class="fixed bottom-4 md:bottom-auto md:top-0 left-1/2 -translate-x-1/2 z-50"
   >
      <!-- Dynamic Island -->
      <div
         data-aos="fade-down"
         aos-data-delay="200"
         class="flex items-center justify-between
   bg-text/95 backdrop-blur-xl
   rounded-full md:rounded-t-none md:rounded-b-4xl
   w-[95vw] md:w-200
   max-w-[95vw]
   px-5 md:px-8
   py-3"
      >
         <!-- Logo -->
         <div class="flex justify-center items-center">
            <a
               class="font-judul text-center text-sm md:text-base font-bold tracking-wider uppercase text-background"
               href="#beranda"
            >
               Pemimpin <br />
               Muda
            </a>
         </div>

         <!-- Desktop Menu -->
         <nav class="hidden md:flex items-center gap-6 font-text">
            <a
               href="#beranda"
               class="font-semibold text-background hover:text-white"
            >
               Beranda
            </a>
            <a
               href="#pilarkarakter"
               class="font-semibold text-background hover:text-white"
            >
               Pilar Karakter
            </a>
            <a
               href="#galeri"
               class="font-semibold text-background hover:text-white"
            >
               Keteladanan
            </a>
            <a
               href="#artikel"
               class="font-semibold text-background hover:text-white"
            >
               Tips
            </a>
         </nav>

         <!-- Desktop CTA -->
         <a
            class="hidden md:inline-block px-1 py-1 bg-primary hover:bg-primary-700 transition-all duration-300 rounded-full hover:scale-105"
            href="#cekkaraktermu"
         >
            <div
               class="px-5 py-1 border-2 border-dashed border-background text-white text-sm font-semibold font-text rounded-full"
            >
               Cek Karaktermu
            </div>
         </a>

         <!-- Hamburger -->
         <button
            id="menuBtn"
            class="md:hidden flex items-center justify-center w-10 h-10 rounded-full bg-background/10"
         >
            <svg
               id="menuIcon"
               xmlns="http://www.w3.org/2000/svg"
               class="w-6 h-6 text-background transition-all duration-300"
               fill="none"
               viewBox="0 0 24 24"
               stroke="currentColor"
            >
               <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M4 6h16M4 12h16M4 18h16"></path>
            </svg>
         </button>
      </div>

      <!-- Floating Menu -->
      <div
         id="mobileMenu"
         class="absolute bottom-20 left-0 right-0 mx-4 opacity-0 scale-95 pointer-events-none transition-all duration-300"
      >
         <div
            class="bg-text/90 backdrop-blur-2xl rounded-3xl p-5 shadow-2xl border border-white/10"
         >
            <nav class="flex flex-col gap-1 font-text">
               <a
                  href="#beranda"
                  class="px-4 py-3 rounded-2xl text-background font-medium hover:bg-white/10 transition"
               >
                  Beranda
               </a>

               <a
                  href="#pilarkarakter"
                  class="px-4 py-3 rounded-2xl text-background font-medium hover:bg-white/10 transition"
               >
                  Pilar Karakter
               </a>

               <a
                  href="#galeri"
                  class="px-4 py-3 rounded-2xl text-background font-medium hover:bg-white/10 transition"
               >
                  Keteladanan
               </a>

               <a
                  href="#artikel"
                  class="px-4 py-3 rounded-2xl text-background font-medium hover:bg-white/10 transition"
               >
                  Tips Kepemimpinan
               </a>

               <a
                  href="#cekkaraktermu"
                  class="inline-block px-1 py-1 bg-primary hover:bg-primary-700 transition-colors rounded-full mt-3"
               >
                  <div
                     class="w-full text-center px-6 py-2 border-2 border-dashed border-background text-white font-semibold font-text rounded-full"
                  >
                     Cek Karaktermu
                  </div>
               </a>
            </nav>
         </div>
      </div>
   </div>
</nav>

<script>
   const menuBtn = document.getElementById("menuBtn");
   const mobileMenu = document.getElementById("mobileMenu");
   const menuIcon = document.getElementById("menuIcon");

   let isOpen = false;

   menuBtn.addEventListener("click", () => {
      isOpen = !isOpen;

      if (isOpen) {
         mobileMenu.classList.remove(
            "opacity-0",
            "scale-95",
            "pointer-events-none",
         );

         menuIcon.innerHTML = `
            <path
               stroke-linecap="round"
               stroke-linejoin="round"
               stroke-width="2"
               d="M6 18L18 6M6 6l12 12"
            />
         `;
      } else {
         mobileMenu.classList.add(
            "opacity-0",
            "scale-95",
            "pointer-events-none",
         );

         menuIcon.innerHTML = `
            <path
               stroke-linecap="round"
               stroke-linejoin="round"
               stroke-width="2"
               d="M4 6h16M4 12h16M4 18h16"
            />
         `;
      }
   });
</script>
